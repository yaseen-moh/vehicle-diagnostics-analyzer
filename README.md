# vehicle-diagnostics-analyzer
A SQL + pandas tool that mines shop repair-ticket data for cascading-failure patterns — flagging vehicles where multiple systems fail within a short mileage window instead of treating each repair as independent. Correctly ranks two seeded project-car failure patterns as the top risks in a 42-vehicle synthetic fleet.
